const Footer = () => {
  return (
    <>
      <p>
        Should the app yield no content, it is likely the API key has reached
        end of life :{"("}
      </p>
    </>
  );
};

export default Footer;
